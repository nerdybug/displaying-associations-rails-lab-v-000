<h1>All Songs</h1>

<ul>
  <% @songs.each do |song| %>
    <li><%= link_to "#{song.artist_name} - #{song.title}", song_path(song) %></li>
    <li><%= link_to "#{song.artist_name} - #{song.title}", artist_path(song.artist_name) %></li>
  <% end %>
</ul>
